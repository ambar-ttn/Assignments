export default function Header() {
  return (
    <header
      style={{
        backgroundColor: "rebeccapurple",
        color: "white",
        padding: "15px",
        textAlign: "center"
      }}
    >
      Hi, I am Header (shared for all pages)
    </header>
  );
}
